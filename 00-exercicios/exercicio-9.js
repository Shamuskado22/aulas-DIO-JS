const numeros = [5, 10, 40, 26, 95, 84, 145, 208, 1026, 312, 1995, 25, 9, 843, 0];

for (let i = 0; i < numeros.length; i++) {
  const numero = numeros[i];
  if (numero % 2 === 0) {
    console.log(numero);
  };
};